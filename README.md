# Fiirmware-code-snippets
C code reusable shorts

Encoder universal reading interrupt focused esp32 idf, vscode. 
change pinout and adapt to your own proyect.

Menu exaples are just example menu implementation with this lib, but read each encoder function in .c for your own app.
